# Cloudflare Tunnel token for this host. Create the tunnel in the Cloudflare
# Zero Trust dashboard (Networks → Tunnels), then copy its token here.
# The SAME token goes on every replica so they share one tunnel.
TUNNEL_TOKEN=
